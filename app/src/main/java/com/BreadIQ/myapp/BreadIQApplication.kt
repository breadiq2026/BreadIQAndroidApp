package com.BreadIQ.myapp

import android.app.Application

/**
 * App-level entry point. Placeholder for now — the iOS app wires
 * Supabase auth/session refresh and RevenueCat purchase configuration
 * here (see `BreadIQApp.swift`). Those integrations land in a later
 * porting pass; see PORTING_PLAN.md.
 */
class BreadIQApplication : Application() {
    override fun onCreate() {
        super.onCreate()
    }
}
