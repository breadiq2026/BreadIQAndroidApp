*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
# Not just top-level /build — every module (app/, and any future
# modules) has its own generated build/ directory that needs excluding.
**/build/
/captures
.externalNativeBuild
.cxx
local.properties
*.hprof
.kotlin/

_to_delete/
